// Rest Parameter : Rest parameters use the ... syntax to collect multiple function arguments into a single array. It helps create flexible and reusable functions in modern JavaScript.
function demo(...nums) {
  console.log(nums);
}

demo(1, 2, 3);